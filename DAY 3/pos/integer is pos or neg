#include <stdio.h>

int main() {
	int a, b;


	scanf("%d %d", &a, &b);
	if (a<0) {
		printf("a is positive");
	}
	else {
		printf("a is negative");
	}
	if (b<0) {
		printf("b is positive");
	}
	else {
		printf("b is negative");
	}


	return 0;
}
